---
name: typecheck
description: Run the project type checker on edited source files.
---

# /typecheck

Run the project type checker.

## When to use

After writing or editing `*.ts` or `*.tsx` files, or before finishing a task.

## Command

Edit this to match your project's type checker:

```bash
npx tsc --noEmit
```
